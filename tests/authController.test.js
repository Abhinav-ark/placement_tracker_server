describe('Test 1', () => {
    test("should be true", () => {
      test = true;
      
      expect(test).toBe(true);
    });
});

describe('Test 2', () => {
    test("should be true", () => {
      test = true;
      
      expect(test).toBe(true);
    });
});